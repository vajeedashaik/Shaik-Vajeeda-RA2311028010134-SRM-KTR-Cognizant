This is a reference copy from the live Maven project at `Solutions/4. Test Driven Dev/src/main/java/com/example/` and `.../src/test/java/com/example/` (files: MyService.java, ExternalApi.java, MyServiceTest.java (see the header comment in MyServiceTest.java: 'Mockito Exercises 1-7' — each test method is labeled with its exercise number)) — run `mvn test` from the project root (Solutions/4. Test Driven Dev) to actually execute it; this copy won't compile standalone since it depends on sibling classes in the same package.
