This is a reference copy from the live Maven project at `Solutions/4. Test Driven Dev/src/main/java/com/example/` and `.../src/test/java/com/example/` (files: RestClient.java, ApiService.java, ApiServiceTest.java) — run `mvn test` from the project root (Solutions/4. Test Driven Dev) to actually execute it; this copy won't compile standalone since it depends on sibling classes in the same package.
