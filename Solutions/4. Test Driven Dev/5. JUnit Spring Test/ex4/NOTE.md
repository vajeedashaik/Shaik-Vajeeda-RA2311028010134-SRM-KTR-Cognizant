This is a reference copy from the live Maven project at `Solutions/4. Test Driven Dev/src/main/java/com/example/` and `.../src/test/java/com/example/` (files: User.java, UserController.java, UserService.java, Application.java, UserDbIntegrationTest.java (see header comment: 'Spring Test - Exercise 4')) — run `mvn test` from the project root (Solutions/4. Test Driven Dev) to actually execute it; this copy won't compile standalone since it depends on sibling classes in the same package.

Note: this test uses no mocks — it exercises the real controller -> service -> repository -> H2 database, per the exercise's "full flow from controller to database" requirement. The service-mocked version (`@MockBean UserService`) lives separately as `UserIntegrationTest.java`, which answers Mock Dependencies - Exercise 3.
