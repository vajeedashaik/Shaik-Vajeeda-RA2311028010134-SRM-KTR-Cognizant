This is a reference copy from the live Maven project at `Solutions/4. Test Driven Dev/src/main/java/com/example/` and `.../src/test/java/com/example/` (files: pom.xml (JUnit dependency setup, no test class for this exercise)) — run `mvn test` from the project root (Solutions/4. Test Driven Dev) to actually execute it; this copy won't compile standalone since it depends on sibling classes in the same package.
