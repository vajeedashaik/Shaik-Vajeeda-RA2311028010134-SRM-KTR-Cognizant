Reference copy from the live project at Solutions/6. Spring Data JPA + Hibernate/orm-learn/ — see 07-Spring-Data-JPA-Hibernate.md in the guide/ folder for how to actually run it (needs MySQL via Docker).

UNCERTAIN MAPPING: this hands-on is a conceptual JPA-vs-Hibernate-vs-Spring-Data-JPA comparison; the docx shows generic EmployeeRepository/EmployeeService snippets as the illustration, so those two files are copied here as the closest best-guess match — no dedicated method in the project implements this exercise specifically.
